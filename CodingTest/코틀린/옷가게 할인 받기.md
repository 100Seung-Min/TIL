### 옷가게 할인 받기
```java
class Solution {
    fun solution(price: Int): Int = floor(price.toFloat() - when(price / 100000) {
        0 -> 0.0
        in 1 until 3 ->  price * 0.05
        in 3 until 5 -> price * 0.1
        else -> price * 0.2
    }).toInt()
}
```