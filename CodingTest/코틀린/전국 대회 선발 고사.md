### 전국 대회 선발 고사
```java
class Solution {
    fun solution(rank: IntArray, attendance: BooleanArray): Int = rank.filterIndexed{i,_ -> attendance[i]}.sorted().slice(0..2).map{rank.indexOf(it)}.let{it[0] * 10000 + it[1] * 100 + it[2]}
}
```