### 마지막 두 원소
```java
class Solution {
    fun solution(num_list: IntArray): IntArray = num_list.plus(if(num_list[num_list.lastIndex] > num_list[num_list.lastIndex - 1]) num_list[num_list.lastIndex] - num_list[num_list.lastIndex - 1] else num_list[num_list.lastIndex] * 2)
}
```