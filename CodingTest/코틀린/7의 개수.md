### 7의 개수
```java
class Solution {
    fun solution(array: IntArray): Int {
        var answer: Int = 0
        array.forEach {
            answer += it.toString().count { it == '7' }
        }
        return answer
    }
}
```