### 개미 군단
```java
class Solution {
    fun solution(hp: Int): Int {
        var captine = hp / 5
        var soldier = (hp - (5 * captine)) / 3
        var normal = hp - (5 * captine) - (3 * soldier)
        return captine + soldier + normal
    }
}
```