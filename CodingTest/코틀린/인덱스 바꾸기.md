### 인덱스 바꾸기
```java
class Solution {
    fun solution(my_string: String, num1: Int, num2: Int): String {
        var temp = my_string[num1]
    return my_string.mapIndexed {i, v -> if (num1 == i) my_string[num2] else if (num2 == i) temp else v}.joinToString("")
    }
}
```