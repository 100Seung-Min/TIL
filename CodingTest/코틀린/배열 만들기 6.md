### 배열 만들기 6
```java
class Solution {
    fun solution(arr: IntArray): IntArray {
        var answer: IntArray = intArrayOf()
        for(i in 0 until arr.size) {
            if(answer.isEmpty()) answer = answer.plus(arr[i])
            else {
                if(answer[answer.lastIndex] == arr[i]) answer = answer.filterIndexed{i,v -> i != answer.lastIndex}.toIntArray()
                else answer = answer.plus(arr[i])
            }
        }
        return if(answer.isEmpty()) intArrayOf(-1) else answer
    }
}
```