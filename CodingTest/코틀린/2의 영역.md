### 2의 영역
```java
class Solution {
    fun solution(arr: IntArray): IntArray = if(arr.indexOfFirst { it == 2 } == -1) intArrayOf(-1) else arr.slice(arr.indexOfFirst { it == 2 }..arr.indexOfLast { it == 2 }).toIntArray()
}
```