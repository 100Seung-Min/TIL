### 배열의 원소만큼 추가하기
```java
class Solution {
    fun solution(arr: IntArray): IntArray {
        var answer = listOf<Int>()
        arr.map { v ->
            (1..v).map {
                answer = answer.plus(v)
            }
        }
        return answer.toIntArray()
    }
}
```