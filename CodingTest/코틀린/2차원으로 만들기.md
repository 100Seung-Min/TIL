### 2차원으로 만들기
```java
class Solution {
    fun solution(num_list: IntArray, n: Int): Array<IntArray> = (0..(num_list.size - n) step n).map { num_list.slice(it until (it + n)).toIntArray() }.toTypedArray()
}
```