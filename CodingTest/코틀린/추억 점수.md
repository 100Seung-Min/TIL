### 추억 점수
```java
class Solution {
    fun solution(name: Array<String>, yearning: IntArray, photo: Array<Array<String>>): IntArray = photo.map { it.map { if (name.contains(it)) yearning[name.indexOf(it)] else 0 }.sum() }.toIntArray()
}
```