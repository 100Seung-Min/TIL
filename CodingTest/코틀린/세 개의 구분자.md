### 세 개의 구분자
```java
class Solution {
    fun solution(myStr: String): Array<String> = myStr.replace("a", "c").replace("b", "c").split("c").filter{!it.isBlank()}.toTypedArray().let{if(it.isEmpty()) arrayOf("EMPTY") else it}
}
```