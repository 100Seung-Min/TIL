### 중복된 문자 제거
```java
class Solution {
    fun solution(my_string: String): String {
        var answer: String = ""
        my_string.forEach {
            if (!answer.contains(it)) {
                answer += it
            }
        }
        return answer
    }
}
```