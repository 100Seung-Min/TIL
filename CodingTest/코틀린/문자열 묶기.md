### 문자열 묶기
```java
class Solution {
    fun solution(strArr: Array<String>): Int {
        var arr = strArr.map{it.length}
        return arr.toSet().maxOfOrNull { arr.count { v -> it == v } } ?: 0
    }
}
```