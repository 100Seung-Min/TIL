### 크기가 작은 부분문자열
```java
class Solution {
    fun solution(t: String, p: String): Int {
        var answer = 0
        for(i in 0..t.length - p.length) {
            if(t.slice(i until i+p.length).toLong() <= p.toLong()) answer++
        }
        return answer
    }
}
```