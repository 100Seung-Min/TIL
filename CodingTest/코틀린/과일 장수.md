### 과일 장수
```java
class Solution {
    fun solution(k: Int, m: Int, score: IntArray): Int = score.sortedDescending().slice(0 until (score.size / m * m)).chunked(m).sumOf { (it.minOrNull() ?: 1) * m }
}
```