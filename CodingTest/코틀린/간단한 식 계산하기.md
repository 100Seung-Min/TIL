### 간단한 식 계산하기
```java
class Solution {
    fun solution(binomial: String): Int = binomial.split(" ").let { when(it[1]) {
        "+" -> it[0].toInt() + it[2].toInt()
        "-" -> it[0].toInt() - it[2].toInt()
        else -> it[0].toInt() * it[2].toInt()
    } }
}
```