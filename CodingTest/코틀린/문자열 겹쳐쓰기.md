### 문자열 겹쳐쓰기
```java
class Solution {
    fun solution(my_string: String, overwrite_string: String, s: Int): String = "${my_string.slice(0 until s)}$overwrite_string${my_string.slice(s + overwrite_string.length until my_string.length)}"
}
```