### 배열 비교하기
```java
class Solution {
    fun solution(arr1: IntArray, arr2: IntArray): Int = if(arr1.size != arr2.size) check(arr1.size, arr2.size) else check(arr1.sum(), arr2.sum())
    
    fun check(a: Int, b: Int): Int = if(a > b) 1 else if(a == b) 0 else -1
}
```