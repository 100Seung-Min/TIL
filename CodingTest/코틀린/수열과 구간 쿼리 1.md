### 수열과 구간 쿼리 1
```java
class Solution {
    fun solution(arr: IntArray, queries: Array<IntArray>): IntArray {
        var answer: IntArray = arr
        queries.forEach {
            (it[0]..it[1]).forEach{
                answer[it]++
            }
        }
        return answer
    }
}
```