### 가장 가까운 같은 글자
```java
class Solution {
    fun solution(s: String): IntArray = s.mapIndexed { i, c -> if (s.indexOfFirst { c == it } == i) -1 else i - s.slice(0 until i).indexOfLast { c == it } }.toIntArray()
}
```