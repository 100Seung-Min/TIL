### 수 조작하기 2
```java
class Solution {
    fun solution(numLog: IntArray): String = (1 until numLog.size).map{
        when(numLog[it] - numLog[it - 1]){
        1 -> "w"
        -1 -> "s"
        10 -> "d"
        else -> "a"
    }}.joinToString("")
}
```