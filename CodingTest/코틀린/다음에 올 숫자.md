### 다음에 올 숫자
```java
class Solution {
    fun solution(common: IntArray): Int =
         if (common[1] - common[0] == common[2] - common[1]) {
            common.last() + (common[1] - common[0])
        } else {
            common.last() * (common[1] / common[0])
        }
}
```