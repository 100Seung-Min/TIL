### 수 조작하기 1
```java
class Solution {
    fun solution(n: Int, control: String): Int = n + control.map { when(it) {
        'w' -> 1
        's' -> -1
        'd' -> 10
        else -> -10
    }}.sum()
}
```