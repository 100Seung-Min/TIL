### 배열 만들기 2
```java
class Solution {
    fun solution(l: Int, r: Int): IntArray = (l..r).filter{it.toString().filter{it != '5' && it != '0'}.length == 0}.toIntArray().let{if(it.isEmpty()) intArrayOf(-1) else it}
}
```