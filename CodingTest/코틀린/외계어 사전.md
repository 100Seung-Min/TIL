### 외계어 사전
```java
class Solution {
    fun solution(spell: Array<String>, dic: Array<String>): Int = if (spell.sorted().joinToString("").let { spellSorted -> dic.map { it.toList().sorted().joinToString("") == spellSorted }}.contains(true)) 1 else 2
}
```