### 문자열이 몇 번 등장하는지 세기
```java
class Solution {
    fun solution(myString: String, pat: String): Int {
        var answer = 0
        for(i in 0..myString.length - pat.length) {
            if(myString.slice(i until i+pat.length) == pat) answer++
        }
        return answer
    }
}
```