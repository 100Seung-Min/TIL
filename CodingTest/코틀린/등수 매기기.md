### 등수 매기기
```java
class Solution {
    fun solution(score: Array<IntArray>): IntArray {
        var rankedList = score.map { (it[0] + it[1]) / 2.0 }.toList()
        return rankedList.map { rankedList.sortedDescending().indexOf(it) + 1 }.toIntArray()
    }
}
```