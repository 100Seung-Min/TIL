### 피보나치 수
```java
class Solution {
    fun solution(n: Int): Int {
        var arr = intArrayOf(0,1)
        for(i in 1 until n){
            arr = arr.plus(((arr[arr.size - 1] + arr[arr.size - 2]).toLong() % 1234567).toInt())
        }
        return arr[arr.size - 1]
    }
}
```